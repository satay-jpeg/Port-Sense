# ---------------------------------------------------------------------------
# Agent provider. Pick ONE key below — PortSense auto-detects which is set.
# Without any key it runs the built-in rule-based router (still fully demoable).
# ---------------------------------------------------------------------------

# Google Gemini — free tier, no credit card. Get a key at:
#   https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override the model. Default is gemini-2.5-flash (free tier).
# Run `npm run models` to list what your key can actually reach.
# GEMINI_MODEL=gemini-2.5-flash

# --- Alternative free providers (same adapter, just a different key) --------
# GROQ_API_KEY=          # https://console.groq.com/keys
# CEREBRAS_API_KEY=      # https://cloud.cerebras.ai
# OPENROUTER_API_KEY=    # https://openrouter.ai/keys
# ANTHROPIC_API_KEY=     # paid; https://console.anthropic.com

# Force a specific provider instead of auto-detecting:
#   gemini | groq | cerebras | openrouter | claude | rules
# AGENT_PROVIDER=gemini

# --- App settings ----------------------------------------------------------
# PORT=3000
# SENSOR_INTERVAL_MS=5000
